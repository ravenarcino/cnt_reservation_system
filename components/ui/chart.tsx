// This default shadcn chart component depended on `recharts`, which is not
// installed in this project (the app uses dependency-free custom charts).
// It is unused; the original recharts-based implementation was removed to
// keep the build green. Safe to delete this file.
export {};
