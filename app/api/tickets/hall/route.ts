import { NextResponse } from "next/server";

// Placeholder route — the hall tickets feature is not implemented yet.
// This stub exists so the file is a valid module (Next.js requires every
// route.ts to export at least one handler).
export async function GET() {
  return NextResponse.json({ data: [], total: 0 });
}
